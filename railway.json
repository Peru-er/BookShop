
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py migrate && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn ecommerce_project.wsgi:application --bind 0.0.0.0:8000"
  }
}
